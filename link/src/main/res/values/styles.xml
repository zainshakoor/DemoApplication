<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base library theme -->
    <style name="Theme.XMLModule" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize library-specific theme attributes if needed -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <!-- Add other theme attributes as needed -->
    </style>


    <style name="button_style">
        <item name="android:layout_height">53dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginStart">20dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:background">@drawable/button_round</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>
